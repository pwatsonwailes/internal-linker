[build]
  command = "rm -rf node_modules package-lock.json && npm install --omit=optional --legacy-peer-deps && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"
  NPM_CONFIG_INCLUDE = "dev"
  NPM_CONFIG_OMIT = "optional"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200